# API base URL (no trailing slash). Required for data.
NEXT_PUBLIC_APP_API_URL=https://api.example.com

# LocalStorage key for admin session token. Set a secret value in production.
NEXT_PUBLIC_APP_ADMIN_SESSION_KEY=newsletter-admin-session

# LocalStorage key for theme preference.
NEXT_PUBLIC_APP_THEME_STORAGE_KEY=newsletter-theme
